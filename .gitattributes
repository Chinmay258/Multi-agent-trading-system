# Normalize line endings: store text as LF in the repo so shell scripts, the
# Makefile, cloud-init, and Dockerfiles work on Linux even when authored on Windows.
* text=auto eol=lf

# Binary assets — never touch.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.joblib binary
*.ico binary
